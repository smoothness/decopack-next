/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'],
  // In Tailwind v4, most configuration is done via CSS variables
  // The config file is now primarily for content paths and plugins
  plugins: [],
}
